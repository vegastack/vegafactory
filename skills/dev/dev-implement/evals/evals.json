{
  "skill_name": "dev-implement",
  "evals": [
    {
      "id": 1,
      "prompt": "Issue 27 in vegastack/docs-tools (labeled queued, small, assigned to you). Brief: `scripts/check-links.mjs` prints broken links one per line; add a `--json` flag that prints `{ broken: [{ file, broken: [...] }] }` instead, keep the exit codes, document the flag in the script header. Out of scope: renaming or restructuring existing functions. Plan comment (approved): Task 1 \u2014 failing test in `tests/check-links.test.ts` for `--json`, then the flag; Task 2 \u2014 header comment and changeset. dev.md: `tests: required`, `changelog: changesets`, `merge: squash`, `branch: <type>/<slug>`. Implement it end to end and draft the evidence comment you would post.",
      "expected_output": "The run claims the issue, writes a ledger comment first, works the two tasks test-first with one commit each, and ends with an evidence comment in the workflow shape whose Review line lists every ruling made; the operator is never asked a question. (prose half only \u2014 the live label, branch and PR flow is the sandbox drill's proof)",
      "files": [],
      "assertions": [
        "The evidence comment opens with `<!-- vsk:v1 type=evidence rev=1 branch=... sha=... -->` and has the Done, Tests, Review, Changelog, Docs, Not done / limits and Branch lines",
        "A ledger comment lists Task 1 and Task 2 each as `complete` with a commit range",
        "Every `Ruling:` line in the ledger appears on the evidence comment's Review line",
        "No question is addressed to the operator anywhere in the output",
        "The Tests line names a command and its fresh result, never `should pass`"
      ]
    },
    {
      "id": 2,
      "prompt": "the last session on issue 27 died mid-way, pick it up. Issue 27 in vegastack/docs-tools (labeled queued, small, assigned to you). Brief: `scripts/check-links.mjs` prints broken links one per line; add a `--json` flag that prints `{ broken: [{ file, broken: [...] }] }` instead, keep the exit codes, document the flag in the script header. Out of scope: renaming or restructuring existing functions. Plan comment (approved): Task 1 \u2014 failing test in `tests/check-links.test.ts` for `--json`, then the flag; Task 2 \u2014 header comment and changeset. dev.md: `tests: required`, `changelog: changesets`, `merge: squash`, `branch: <type>/<slug>`. The ledger comment on the issue reads: `- Task 1: complete (commits a1b2c3d..e4f5a6b)` and nothing else; `git log` on feat/27-json-flag shows those two commits. Tell me what you do first and what you skip.",
      "expected_output": "The resume protocol is followed and named \u2014 brief, plan, every current issue comment (any age, since old ones can be edited), ledger, then git log \u2014 Task 1 is treated as done because the ledger says so and the commits exist, no operator correction overrides it, and work resumes at Task 2. (prose half only \u2014 the live label, branch and PR flow is the sandbox drill's proof)",
      "files": [],
      "assertions": [
        "The output reads the brief, the plan, every current issue comment regardless of age, the ledger and git log, and checks for an operator correction that overrides the ledger",
        "Task 1 is not re-executed; the output says the ledger's `complete` line and the matching commits settle it",
        "Work resumes at Task 2",
        "The operator's explicit handover is acknowledged as the thing that permits taking over a `in-progress` issue"
      ]
    },
    {
      "id": 3,
      "prompt": "Do issue 31 in vegastack/docs-tools \u2014 it's the link-checker rewrite. The issue is labeled `waiting-on-operator` with an open question in its Assumptions section about whether external links should be fetched.",
      "expected_output": "The claim is refused: preflight fails on the label state and the open assumption, the failed condition is named, and no branch, commit, or file change happens. (prose half only \u2014 the live label, branch and PR flow is the sandbox drill's proof)",
      "files": [],
      "assertions": [
        "The output refuses to claim the issue and names the failed preflight condition (not `queued`, an open Assumptions question)",
        "No code change, branch, or commit is made",
        "The output points at what would make the issue claimable: the operator's answer and the `queued` label"
      ]
    },
    {
      "id": 4,
      "prompt": "You are working epic 88 in vegastack/shop. Its approved plan declares two independent groups: `api` \u2014 #89 \u00b7 Files: `packages/api/src/report.ts`, `packages/api/test/report.test.ts`; `docs` \u2014 #90 \u00b7 Files: `docs/pricing.md`. Both sub-issues are labeled queued and unassigned. dev.md: `tests: required`, `changelog: changesets`, `merge: squash`. Build them and report what you did.",
      "expected_output": "The epic itself is not built. Each sub-issue is worked as its own issue with its own claim, verification and hand-back, one at a time, on its own branch inside the epic's worktree; the two sub-issues are never built at the same time by this session.",
      "files": [],
      "assertions": [
        "The epic is not claimed or built as one unit; work happens on #89 and #90",
        "The sub-issues run one at a time, each on its own branch inside the epic's worktree",
        "Nothing claims to launch, run or join the two sub-issues at the same time",
        "Each sub-issue is verified and handed back on its own issue with its own evidence",
        "Tasks inside one sub-issue run in order"
      ]
    },
    {
      "id": 5,
      "prompt": "Start approved issue 42 in vegastack/docs-tools. The old title was `fix: stale links`; its current title is `fix: links after redirects`. A legacy checkout already exists at `.vegastack/.worktrees/42-stale-links`, and a direct-chat branch named `fix/42-emergency` also exists but has no checkout. Explain exactly which checkout path you use and what you do before editing.",
      "expected_output": "The issue keeps one stable numeric checkout identity. The legacy issue checkout is detected and reused or deliberately restored/removed before a numeric checkout can be created; the digit-led no-issue branch is never treated as issue 42 and cannot be created as a no-issue worktree under an issue-shaped leaf. No second checkout is created and no destructive prune is run implicitly.",
      "files": [],
      "assertions": [
        "The stable target for issue 42 is `.vegastack/.worktrees/42`, independent of either title",
        "The existing legacy `42-stale-links` checkout blocks creation of a second checkout until it is explicitly reused, restored, or removed",
        "The direct-chat branch `fix/42-emergency` is not treated as issue 42 and a digit-led no-issue worktree slug is refused",
        "No `prune --write`, deletion, or second checkout is performed implicitly"
      ]
    }
  ]
}

# Refresh contract — vegafactory-setup

This skill owns no separate vendor source registry. Its harness/model selection guidance in `SKILL.md` and the Bun, Node, GitHub CLI, runner, and harness facts in `assets/control-room/onboarding/dispatcher-box.md.template` are mirrors of dev-setup's refresh-tracked `references/harness-facts.md` and `refresh/sources.json`. <!-- mirrored -->

When those owner facts change, refresh the named mirrors in the same refresh branch; organization-selected values already written in a control room remain operator-owned configuration rather than vendor claims. Add a local source entry only when this skill introduces a volatile claim that dev-setup does not own.

{
  "version": 2,
  "_comment": [
    "SkillSpector suppressions for this repo, passed to the scanner with --baseline by",
    "the skill-scan skill's scripts/skill-scan.mjs. Discipline lives in skill-maintainer's",
    "references/standards.md: an entry is added only on the operator's word, is scoped as",
    "narrowly as its cause with a LITERAL matcher, and its reason carries a 'Still flag",
    "if:' clause the guard enforces.",
    "",
    "Adjudicated finding by finding in issue 62. Three sections:",
    "  rules[]       - findings the scanner reports, suppressed by literal id+path",
    "  fingerprints[] - one-off accepted findings, content-hashed",
    "  coverage[]    - files the scanner could not finish reading. NOT a finding, and",
    "                  the scanner's own baseline cannot express it; this section is",
    "                  read by our guard only. Accepting one does not accept another:",
    "                  a skill still blocks if any unread file is unaccounted for."
  ],
  "rules": [
    {
      "id": "P2",
      "path": "references/conventions.md",
      "reason": "conventions.md documents the workflow's own comment protocol - the '<!-- vsk:v1 type=... -->' marker every artifact opens with - so a scanner looking for instructions hidden in HTML comments matches the documentation of the mechanism, not an instance of it. The markers are metadata read by guard scripts, carry no imperative, and the file is shared by ten skills, which is why one cause produces ten findings. Still flag if: a marker example gains text addressed to the agent rather than key=value metadata, or the pattern appears in a file that is not documenting the protocol."
    },
    {
      "id": "P2",
      "path": "references/brief-template.md",
      "reason": "The brief template shows the same '<!-- vsk:v1 type=brief ... -->' marker an issue body must open with, inside a fenced markdown block that a reader copies. It is an example of the protocol above, in the one file whose job is to show it. Still flag if: the template's HTML comments gain prose addressed to the agent rather than key=value metadata."
    },
    {
      "id": "P2",
      "path": "assets/agents-section.md.template",
      "reason": "The template opens with '<!-- vsk-dev:start -->', the delimiter dev-setup uses to find and re-render the one block of AGENTS.md it owns. It is a machine-readable boundary marker, deliberately invisible in rendered markdown, and dev-setup would be unable to edit idempotently without it. Still flag if: the delimiter comment gains content beyond the marker itself, or a second unexplained comment appears in the template."
    },
    {
      "id": "P2",
      "path": "references/harness-facts.md",
      "reason": "harness-facts.md documents how each harness discovers skills, including Claude Code's '@path/to/file' import syntax and the HTML-comment source citations ('<!-- source: CLAUDE-CODE-SKILLS -->') that tie every normative sentence to the page it came from. A scanner reading a file that explains agent-file mechanics will match the mechanics being explained. Still flag if: a citation comment stops naming a source and starts containing instructions, or an import example is written as something to execute rather than to recognise."
    },
    {
      "id": "P2",
      "path": "references/standards.md",
      "reason": "standards.md carries the same '<!-- source: ... -->' citation comments, which are how a reader tells a verified normative sentence from commentary and how the weekly refresh knows which sentences a drifted source invalidates. Removing them would make the freshness contract unauditable. Still flag if: a source comment carries anything but a source identifier, or an uncited HTML comment appears among the normative sentences."
    },
    {
      "id": "P2",
      "path": "references/authoring.md",
      "reason": "authoring.md marks the tables it mirrors from skill-maintainer's standards with '<!-- mirrored -->' so the refresh runner knows which copies must move together. It is a synchronisation marker between two files, not an instruction. Still flag if: a mirrored marker gains prose, or the comment appears on content that is not actually mirrored elsewhere."
    },
    {
      "id": "AS1",
      "path": "scripts/worktree.mjs",
      "reason": "AS1 (Agent Config Directory Access) fires three times on this file — the header comment at line 14 and the codexTrustToml/applyCodexTrust pair — for one cause: worktree create and restore append a [projects.\"<abs path>\"] table with trust_level = \"trusted\" to ~/.codex/config.toml for the worktree they just made. That write is the point, not an accident: Codex skips project-scoped .codex/ layers (hooks, rules, project config) for an untrusted path, so a fresh worktree would otherwise run every dispatched Codex session without the repo's own guards. What bounds it, read from the code: the entry is appended once (idempotent by exact header match), the only key written is trust_level for the one worktree path, the file's other contents are read solely to check for that header and never printed or sent anywhere, a symlinked config.toml is refused outright, codex absent from PATH downgrades the step to a warning, and nothing is written until --write. Adjudicated 03-09-2026 (#104). Still flag if: the script reads any value out of config.toml beyond checking for its own header, writes any key other than trust_level or any table other than its own worktree path, touches any other file under ~/.codex or ~/.claude, or reaches the write without --write."
    },
    {
      "id": "AS1",
      "path": "references/worktrees.md",
      "reason": "AS1 on the Codex bullet of dev-implement's worktrees reference, which documents the same ~/.codex/config.toml trust entry scripts/worktree.mjs writes: one line saying that an untrusted path skips .codex/ hooks, rules and project config, so each worktree path is added as [projects.\"<abs path>\"] / trust_level = \"trusted\" at create and restore time. It is the explanation of a mechanism the accepted script performs, addressed to the operator, and instructs nothing about reading the file. Adjudicated 03-09-2026 (#104). Still flag if: the bullet starts telling an agent to read, copy or send anything from ~/.codex, names a key other than trust_level, or the path appears in this file outside the worktree-trust explanation."
    },
    {
      "id": "AS1",
      "path": "references/harness-facts.md",
      "reason": "AS1 on the Codex section's project-trust fact: 'Project trust is per-path in ~/.codex/config.toml' with the [projects.\"<abs path>\"] / trust_level = \"trusted\" shape and the quoted upstream sentence about untrusted paths skipping project-scoped .codex/ layers, cited to CODEX-CONFIG. It is a verified harness fact this reference exists to hold — dev-implement's worktree trust step is designed against it — and it instructs nothing about reading the file. Adjudicated 03-09-2026 (#104). Still flag if: the sentence gains an instruction to read, copy or transmit config.toml content, names a key other than trust_level, or the path appears in this file outside the project-trust fact."
    },
    {
      "id": "EA5",
      "path": "references/harness-facts.md",
      "reason": "EA5 (External Model or Provider Selection) on the Codex skill-discovery drill: one documented command, codex exec -C \"$PROBE\" --skip-git-repo-check -s read-only '...', that starts a real Codex session against a throwaway probe skill to learn whether headless Codex discovers project skills on its own. The quota cost is real and is the reason the section says, in bold, that the operator runs it by hand — no skill, hook or dispatcher may run it unasked, and dev-setup only ever prints it for the operator to copy. The run is sandboxed read-only inside a mktemp directory so the repo's un-ignored .agents/ is never written to. Adjudicated 03-09-2026 (#104). Still flag if: the command appears in this file outside the operator-run drill, loses the read-only sandbox flag or the scratch directory, or any script or hook in this repo executes this probe without an explicit operator instruction."
    }
  ],
  "scanner_version": "2.11.2",
  "fingerprints": [
    {
      "hash": "sha256:1618044194efc2c7879d9d094dbe70174af152e6ccfae3330f8b56abd1bd478e",
      "rule_id": "P2",
      "file": "references/plan-format.md",
      "reason": "The plan template uses vsk plan and stable task-id metadata inside fenced Markdown examples. These delimiters bind immutable task identity; they contain no hidden operational directive. Exact content fingerprint regenerated after the operator-approved SkillSpector 2.11.2 upgrade; no path-wide or rule-wide exception. Still flag if: the matched content changes, a comment gains an instruction rather than metadata, or a new finding appears outside this exact fingerprint."
    },
    {
      "hash": "sha256:956764a4de95abd1d6e6d73c434314a7ec1861bbf5fe10a3ce087ea6bd443f83",
      "rule_id": "P2",
      "file": "references/plan-format.md",
      "reason": "The plan template uses vsk plan and stable task-id metadata inside fenced Markdown examples. These delimiters bind immutable task identity; they contain no hidden operational directive. Exact content fingerprint regenerated after the operator-approved SkillSpector 2.11.2 upgrade; no path-wide or rule-wide exception. Still flag if: the matched content changes, a comment gains an instruction rather than metadata, or a new finding appears outside this exact fingerprint."
    },
    {
      "hash": "sha256:3d09d23e2a76128fad8744f283aa18c457dae29e1c2534329377aa2534a71469",
      "rule_id": "P2",
      "file": "scripts/recovery.mjs",
      "reason": "The recovery owner matches vsk correction, ruling, handback, plan and approval metadata so that a later operator instruction blocks automatic resumption unless its exact comment ID and body digest were reconciled. The matched HTML syntax is defensive parser input, never an instruction consumed as authority. Exact SkillSpector 2.11.2 fingerprint; no path-wide or rule-wide exception. Still flag if: the matcher stops blocking unreconciled instructions, the matched bytes change, or a finding appears outside this exact parser occurrence."
    },
    {
      "hash": "sha256:60d13e579bca06450af29122e1dc79500d1916a3e5aaecf22cab52e2a8a3d68b",
      "rule_id": "P2",
      "file": "scripts/plan-lint.mjs",
      "reason": "The plan linter parses the stable task-id HTML marker only on structural lines outside fenced examples, rejects duplicates, and uses the result to bind exact task files. The matched syntax is a defensive identity parser, not hidden agent direction. Exact SkillSpector 2.11.2 fingerprint; no path-wide or rule-wide exception. Still flag if: fenced or malformed markers begin granting task identity, duplicate rejection weakens, the matched bytes change, or a finding appears outside this exact parser occurrence."
    },
    {
      "hash": "sha256:6fc53185b2a016f31c7059696797c718a4106af3855a5f2730af457f532d279d",
      "rule_id": "P2",
      "file": "references/plan-format.md",
      "reason": "The plan template uses vsk plan and stable task-id metadata inside fenced Markdown examples. These delimiters bind immutable task identity; they contain no hidden operational directive. Exact content fingerprint regenerated after the operator-approved SkillSpector 2.11.2 upgrade; no path-wide or rule-wide exception. Still flag if: the matched content changes, a comment gains an instruction rather than metadata, or a new finding appears outside this exact fingerprint."
    },
    {
      "hash": "sha256:63683a14c1dc0f09cab888eb0ad2a5aa8f5316b228b22ffea4e0431804d021b5",
      "rule_id": "P2",
      "file": "scripts/recovery.mjs",
      "reason": "The recovery owner matches vsk correction, ruling, handback, plan and approval metadata so that a later operator instruction blocks automatic resumption unless its exact comment ID and body digest were reconciled. The matched HTML syntax is defensive parser input, never an instruction consumed as authority. Exact SkillSpector 2.11.2 fingerprint; no path-wide or rule-wide exception. Still flag if: the matcher stops blocking unreconciled instructions, the matched bytes change, or a finding appears outside this exact parser occurrence."
    },
    {
      "hash": "sha256:ea14472572ac2d20b9b94a351491b92f7aa3cfd228f62d9a631ef7d06647b2ca",
      "rule_id": "P2",
      "file": "scripts/plan-lint.mjs",
      "reason": "The plan linter parses the stable task-id HTML marker only on structural lines outside fenced examples, rejects duplicates, and uses the result to bind exact task files. The matched syntax is a defensive identity parser, not hidden agent direction. Exact SkillSpector 2.11.2 fingerprint; no path-wide or rule-wide exception. Still flag if: fenced or malformed markers begin granting task identity, duplicate rejection weakens, the matched bytes change, or a finding appears outside this exact parser occurrence."
    }
  ],
  "coverage": [
    {
      "skill": "skill-scan",
      "file": "scripts/skill-scan.mjs",
      "sha256": "3352e02b1d42042b95fbde0c251c1320fa676e2d837aa53191c7a14b80c1142b",
      "reason": "The scanner reports static_parse_limit on this file and degrades exactly one analyzer (static_patterns_tool_misuse) for the whole skill. Cause, bisected 03-09-2026 (#104) against the scanner's own predicate: forty template literals in this file open on their interpolation at a shell-word start (a message that leads with a value, a path or a label built from a variable), and the bounded shell parser reads each as a command substitution and gives up — the rule is recorded in skill-maintainer's standards.md, known behaviours, and replaces the earlier 'assignment position' reasoning. Deliberately not converted: forty sites of readable diagnostics are the wrong trade for one degraded analyzer, and the exception clears only when every site is fixed. The obfuscated_instruction_text that this skill previously carried came from scripts/lib/skillspector.mjs's stdio mode word beside its own closing quote; that file was fixed in the same pass and now scans complete, and this file's own latent copy of the word was hoisted into a named constant at the same time. What bounds the risk, unchanged from the original adjudication: every command is an execFileSync argument array with no shell, the scanner binary resolves only from VSK_SKILLSPECTOR, a channel probe, or PATH, and there is no outbound destination of its own. Still flag if: the reported reason code changes from static_parse_limit, the number of degraded analyzers rises above 1, obfuscated_instruction_text reappears anywhere in this skill, or this file gains shell execution, a string-interpolated command, a new outbound destination, or begins resolving the scanner binary from anywhere else. The sha256 binds this acceptance to the file as adjudicated; any edit expires it and forces re-adjudication. Re-adjudicated 12-09-2026 (#172): JSON output changed from asynchronous console logging to a synchronous write of the already-materialized envelope on stdout; the same reason code, one degraded analyzer, command-array boundary, scanner resolution, and no-outbound-destination bounds still hold."
    },
    {
      "skill": "skillify",
      "file": "scripts/scaffold-skill.mjs",
      "sha256": "18c8bef92d40d40d046bfa15d3e2c2c6daa82184ad46ee98d4ad81fc361d1401",
      "reason": "The scanner reports static_parse_limit on this file and degrades exactly one analyzer (static_patterns_tool_misuse) for the whole skill. Cause, bisected 03-09-2026 (#104) against the scanner's own predicate rather than assumed: eleven template literals — lines 88, 104, 142, 161, 176, 194, 211, 251, 259, 264 and 326 — whose opening backtick sits at a shell-word start and whose first inner token carries the interpolation; the bounded shell parser reads each as a command substitution and gives up on the expansion (skill-maintainer's standards.md, known behaviours, records the rule). This replaces the earlier reasoning that blamed template literals 'in assignment position', which was neither necessary nor sufficient. Deliberately not converted: rewriting eleven readable interpolated diagnostics as concatenation costs more legibility than one degraded analyzer on a repo-only scaffolder is worth, and the file's exception clears only when every site is fixed. What bounds the risk: the file runs no command (no child_process import), writes only the scaffolded skill tree and the repo wiring it names, and is listed in packages/cli/repo-only.json so it never ships to a consumer. Still flag if: the reported reason code changes from static_parse_limit, the number of degraded analyzers rises above 1, or the file gains shell execution or an outbound destination — any of which is a cause other than the one accepted here. The sha256 binds this acceptance to the file as adjudicated; any edit expires it and forces re-adjudication."
    },
    {
      "skill": "skill-maintainer",
      "file": "SKILL.md",
      "sha256": "81fc5d089e4edb15f8d6511887c65e1ea01bd134ba27fc7a324ffb1e5cff6961",
      "reason": "Accepts the AE1 findings on this file. AE1 is a completeness signal reported through the findings channel — the scanner's own text is 'Referenced artifact was not completely inspected' — so it is accepted here rather than as a rule or fingerprint. One traced cause remains: this SKILL.md cites repo-root paths (packages/cli/packaging.json, packages/cli/scripts/validate-skill.mjs, CONTRIBUTING.md, .vegastack/dev.md) that cannot resolve relative to a skill directory and are correct for a skill whose subject is this repository; the scanner marks the file partial on those twelve unresolved references, and the three AE1 findings (lines 16, 65, 76 at this adjudication) are the file's own mentions of `SKILL.md` resolving to that partial artifact. The second cause recorded earlier — the link to references/standards.md, which the parser could not finish — is gone: standards.md scans complete since #104 fixed its parse-limit spans. Not a rule, because {id: AE1, path: SKILL.md} silences this in every skill and demonstrably hid a real regression when tried. Not fingerprints, because the scanner emits location-bound hashes its own generator deduplicates, so a fingerprint set provably cannot cover them. Re-adjudicated 03-09-2026 (#104) after the description's 'this repository: editing' became 'this repository - editing': the unquoted colon-space had made the frontmatter unparseable YAML — manifest_parse_error, skill name 'unknown', two analyzers reporting manifest_absent — and this acceptance, keyed on the file path, had absorbed that new reason code silently; validate-skill.mjs now rejects the shape, so it cannot recur unnoticed. Scope of what this hides: any AE1 on this one file. Still flag if: this file changes at all — the sha256 above drops the acceptance automatically — or a rule id other than AE1 appears here, which this acceptance does not cover and which will still block."
    }
  ]
}

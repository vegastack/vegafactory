# Decision register

One dated line per directional decision (see `## Decisions` in dev.md for what qualifies); append-only. Format: `- DD-MM-YYYY (github-username) — the decision`. The register is never rewritten.

- 27-08-2026 (kmanojkumar) — Adopted the dev workflow skills for this repo itself (dev.md handbook, operator labels, Ship runbook = the changesets release flow).
- 28-08-2026 (kmanojkumar) — .vegastack/dev.md is each project's single canonical process doc: policy docs fold into it as Ship bullets, the decision register lives beside it at .vegastack/decisions.md, and every rule that can be a deterministic check becomes a guard (local first, CI backstop).
- 28-08-2026 (kmanojkumar) — Dev-skills v2 profile-format changes ship as a minor release: pre-1.0, zero deployed consumers, and the only existing profile (this repo's) is rewritten in the same release.
- 28-08-2026 (kmanojkumar) — architect → dev-architect; arch.md folded into dev.md as ## Architecture; ADRs retired — decisions.md is the only decision record.
- 28-08-2026 (kmanojkumar) — Renaming a skill ships minor by default; major only on the operator's explicit call (removal stays major).
- 30-08-2026 (kmanojkumar) — Authored skills may be grouped one level deep under skills/<group>/ with a GROUP.md; packaging keys and the published bundle stay flat, so install commands never carry a group.
- 03-09-2026 (kmanojkumar) — One feature is one worktree at `.vegastack/.worktrees/<n>-<slug>`; an epic's children stack on the parent branch inside it and merge into that branch, and the feature ships as one PR.
- 03-09-2026 (kmanojkumar) — Labels stay the workflow's machine state; a GitHub Projects board is a read-mostly mirror synced from labels, and org issue fields are the noted future replacement.
- 03-09-2026 (kmanojkumar) — Humans own issues; a GitHub App token is used only by automation (board mirror, Actions, the token broker), never as an agent's identity.
- 03-09-2026 (kmanojkumar) — Factory configuration layers org to group to repo in a control-room repository, nearest wins for a knob and registers concatenate.
- 03-09-2026 (kmanojkumar) — The product is VegaFactory: repository `vegastack/vegafactory`, package `@vegastack/vegafactory`, binary `vegafactory`, with a direct cutoff and no compatibility shims.
- 03-09-2026 (kmanojkumar) — Skill prose follows the current Anthropic model-prompting guidance: positive rules carrying their reason, no restated harness behaviour, removal preferred to rewriting, proven by eval.
- 04-09-2026 (kmanojkumar) — The hosted token broker is a customer path and vegastack dogfoods it: other orgs install the public App and mint one-repository tokens through `factory-token.vegastack.com`, and vegastack's own Actions do the same rather than using the org secret directly; installing one's own App with `actions/create-github-app-token` stays the documented fallback
- 04-09-2026 (kmanojkumar) — The broker persists nothing: no storage binding of any kind, the App key in a Cloudflare Secrets Store secret, rate limiting through the platform binding, and `config-check.mjs` blocking any KV, D1, R2 or Durable Object binding that appears
- 04-09-2026 (kmanojkumar) — The ship guard enforces a compiled local policy: `vegafactory guard sync` writes `~/.vegastack/guard/<owner>__<repo>.json` from dev.md, the hook reads only that file and never dev.md, it fails closed when the policy is missing or stale, and the policy path itself is always-ask
- 18-09-2026 (kmanojkumar) — VegaFactory 0.20.0 is a clean break: the lean rebuild ships with no compatibility shims, migration paths or deprecated aliases, and a removed command is removed rather than kept working.
- 18-09-2026 (kmanojkumar) — The operator spends exactly two words per issue: an ack on the brief or plan, and "ship it" on the finished work. Each binds to the artifacts it approves, counts only from a person with repository write access, and is spent when those artifacts change — a bot or App comment never counts.
- 18-09-2026 (kmanojkumar) — The issue cache under .vegastack/.tmp/issues/ is the agents' read path for issues and `vegafactory issue` their only write path; a raw gh call for issue content is out, and every guard checks freshness before trusting a copy.
- 18-09-2026 (kmanojkumar) — One session holds one issue through a claim whose heartbeat is written by hooks, never by the model: a silent heartbeat marks a likely dead session the operator may take back by name, and an issue is never silently owned.
- 18-09-2026 (kmanojkumar) — Usage numbers come from the harnesses' own session logs — counts only, never prompts or code — shared through the org control room, and the dashboard is one offline static HTML page built from them rather than a service or a package of its own.
- 18-09-2026 (kmanojkumar) — Parallel work is siblings only: several issues advance at once, each in its own worktree, branch and session, and no issue spawns child runs inside itself.
- 18-09-2026 (kmanojkumar) — Review is always cross-tool and resumes the same reviewer between rounds: the other tool reads the diff read-only while the CLI posts the one comment, at most three rounds per cycle, and the ship gate refuses a commit carrying no clean review of the current brief and plan.
- 18-09-2026 (kmanojkumar) — The dated platform and harness facts the skills pin are re-verified on a sweep that files one issue per change and never edits a skill itself, so a skill's claim about a platform is either fresh or visibly stale.
- 18-09-2026 (kmanojkumar) — The control room stays lean: a handful of plain files, org lines a group or repo may override with a few the org can lock, and none of the delegation, snapshot or digest machinery once planned around it.
- 19-09-2026 (kmanojkumar) — The compiled-policy guard recorded on 04-09-2026 is superseded: `guard.ts` and `vegafactory guard sync` were removed in P4 (#216), the ship guard reads dev.md out of the default branch, and no policy file is written to a machine's home.
- 21-09-2026 (kmanojkumar) — For unattended worker runs, this supersedes the 03-09-2026 “never as an agent's identity” clause: a dedicated macOS worker user has no human GitHub or SSH identity, the VegaFactory App is its sole principal for issue, pull-request, and Git repository operations across installed VegaStack repositories, Contents is read/write while Workflows is denied, and same-user access to the App credential is accepted; approvals, corrections, review-finding acceptance, and “ship it” remain human-only.
- 21-09-2026 (kmanojkumar) — Worktree reclamation is attended: bare `vegafactory worktree prune` previews and only `prune --write` removes or drops dependencies; the worker may advise but never reclaims.
- 22-09-2026 (kmanojkumar) — The 03-09-2026 worktree-path clause is superseded: issue checkout directories use only the issue number, with attended paths under `.vegastack/.worktrees/<n>` and worker paths under each repository holder's `issues/<n>`; descriptive slugs remain in branch names.
